{{#*inline "page"}}
    <div>
        <p>debug: {{ foo }}</p>
        {{#each weeks}}
        <p>{{markdown this.body}}</p>
        {{else}}
        <p>This page intentionally left blank.</p>
        {{/each}}
    </div>
{{/inline}}
{{> template }}
