{{#*inline "page"}}
    <div>
        {{#each weeks}}
        <p>{{this}}</p>
        {{else}}
        <p>This page intentionally left blank.</p>
        {{/each}}
    </div>
{{/inline}}
{{> template }}
